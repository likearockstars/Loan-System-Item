package model;

import java.time.LocalDateTime;

public interface LoanService {

	/**
	 * Method responsible for being a generic loan items
	 * 
	 * @param item
	 * @param person
	 */
	void static void loanItem(Item item, User user,LocalDateTime returnDate);
	void static  void returnItem(Item item,User user);
}
