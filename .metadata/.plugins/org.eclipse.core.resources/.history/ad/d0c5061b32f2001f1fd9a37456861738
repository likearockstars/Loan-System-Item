package model;

public interface LoanInformationServcice {
	
	/**
	 * Method responsible for returning the person's loan history
	 * @param person
	 * @return returns a string containing loan information
	 */
	String getInformation(Person person);
}
